<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kgc.dao.SysUserMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.kgc.entity.SysUser">
        <id column="userId" property="userid" />
        <result column="userName" property="username" />
        <result column="password" property="password" />
        <result column="age" property="age" />
        <result column="six" property="six" />
        <result column="createDate" property="createdate" />
        <result column="createBy" property="createby" />
        <result column="updateDate" property="updatedate" />
        <result column="updateBy" property="updateby" />
        <result column="isDel" property="isdel" />
        <result column="isAdmin" property="isadmin" />
        <result column="nickName" property="nickname" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        <if test="true">
        userId, userName, password, age, six, createDate, createBy, updateDate, updateBy, isDel, isAdmin, nickName
        </if>
    </sql>
    <select id="existUser"  resultType="com.kgc.entity.SysUser">
        select userId, userName, password, age, six, createDate, createBy, updateDate, updateBy, isDel, isState, nickName, email    from sys_user
        where
            userName = #{userName}
          and isDel = 0
    </select>
    <select id="login"  resultType="com.kgc.entity.SysUser">
        select userId, userName, password, age, six, createDate, createBy, updateDate, updateBy, isDel, isState, nickName, email    from sys_user
        where
            userName = #{userName}
          and password = #{password}
          and isDel = 0
    </select>
</mapper>
